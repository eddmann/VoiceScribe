Prefer `make` targets over direct commands. Run `make help` to discover available targets.
Run `make can-release` to verify changes before finishing.
Use `make dev` to test a local app build quickly.
Detroit-style tests: assert on observable behaviour and mock only at the boundary.
